<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="file">
	<sql id="column">
		num, writer, title, orgFileName, saveFileName, fileSize, regdate
	</sql>

	<select id="getList" parameterType="map" resultType="com.example.boot11.dto.FileDto">
		SELECT result.*, ROWNUM
		FROM (SELECT <include refid="column"/>
		 	  FROM board_file
		 	  ORDER BY num desc) result
		WHERE ROWNUM between #{startRowNum, jdbcType=INT} and #{endRowNum, jdbcType=INT}
	</select>
	
	<delete id="delete" parameterType="int">
		DELETE FROM board_file
		WHERE num = ${int}
	</delete>
	
	<insert id="insert" parameterType="fileDto">
		INSERT INTO board_file
		(<include refid="column"/>)
		VALUES(board_file_seq.NEXTVAL, #{writer}, #{title}, #{orgFileName},
			   #{saveFileName}, #{fileSize}, sysdate)
	</insert>
	
	<select id="select" parameterType="int" resultType="fileDto">
		SELECT <include refid="column"/>
		FROM board_file
		WHERE num = #{int}
	</select>
	
	<select id="getCount" resultType="int">
		SELECT NVL(MAX(ROWNUM), 0)
		FROM board_file
	</select>
</mapper>